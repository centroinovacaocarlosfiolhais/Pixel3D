services:
  - type: web
    name: pixel3d-galeria
    runtime: node
    buildCommand: npm install --prefix galeria
    startCommand: node galeria/server.js
    plan: free
    envVars:
      - key: UPLOAD_PASSWORD
        sync: false
      - key: NODE_ENV
        value: production
